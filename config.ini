[output]
; Directory where downloaded PDFs are saved
directory = ./downloads

[account]
; Path to a file holding Scribd cookies, used to download premium documents.
; Either a JSON cookie array (e.g. exported from a browser extension) or a raw
; "name=value; name2=value2" header string. If unset, only public documents work.
cookies_file =
